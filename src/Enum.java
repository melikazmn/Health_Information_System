public class Enum {
    public enum Sex {Female, Male}

    public enum Mode {VIP, Normal, Insurance}

    public enum User {Admin, Physician, Nurse, Patient}

    public enum State {Picked, NotPicked, Discharged}

}
